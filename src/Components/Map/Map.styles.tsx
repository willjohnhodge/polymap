import styled from 'styled-components';

export const StyledMapWrapper = styled.div`
  display: block;
  height: 100%;
  max-width: 100%;
  position: relative;
  overflow: hidden;

  .mapbox-improve-map {
    display: none;
  }
`;
